<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.deltav</groupId>
        <artifactId>delta-v-parent</artifactId>
        <version>1.2.0-alpha3</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <groupId>org.opennms.core</groupId>
    <artifactId>org.opennms.core.model-jakarta</artifactId>
    <name>OpenNMS :: Core :: Model Jakarta</name>
    <description>
        Jakarta Persistence entity classes for Spring Boot 4 daemons.
        Same package (org.opennms.netmgt.model) as legacy opennms-model,
        different module. Only one should be on the classpath at runtime.
    </description>

    <dependencies>
        <!-- Jakarta Persistence API (from Spring Boot 4 BOM) -->
        <dependency>
            <groupId>jakarta.persistence</groupId>
            <artifactId>jakarta.persistence-api</artifactId>
        </dependency>

        <!-- Hibernate 7 core — for @Filter, @Formula, @SQLRestriction annotations -->
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-core</artifactId>
        </dependency>

        <!-- AbstractDaoJpa base class (extracted from daemon-common to break cycle) -->
        <dependency>
            <groupId>org.deltav.core</groupId>
            <artifactId>org.opennms.core.dao-jpa-support</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- DAO interfaces (AlarmDao, NodeDao, DistPollerDao, ServiceTypeDao) -->
        <!-- CategoryDao.getCriterionForCategorySetsUnion returns List<Criterion> (Hibernate 3.x type).
             CategoryDaoJpa.getCriterionForCategorySetsUnion throws UnsupportedOperationException but
             the compiler still needs the type. org.hibernate:hibernate-core (3.x) does not come
             through transitively because upstream exclusions in opennms-model and opennms-dao-api
             strip it before Maven's resolver sees it; the provided-scope dep below supplies it only
             at compile time. -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-dao-api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
                <exclusion><groupId>org.hibernate</groupId><artifactId>hibernate-core</artifactId></exclusion>
                <exclusion><groupId>org.hibernate.javax.persistence</groupId><artifactId>hibernate-jpa-2.0-api</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- Events API (EventConstants, EventForwarder, EventDatabaseConstants) -->
        <dependency>
            <groupId>org.opennms.features.events</groupId>
            <artifactId>org.opennms.features.events.api</artifactId>
        </dependency>

        <!-- core.model-api supplies compile-time helper types used by our ported
             entities and DAO classes: OnmsSeverity, PrimaryType, HeatMapElement,
             FilterManager, SurveillanceStatus, ServiceSelector, OutageSummary,
             AlarmSummary/SituationSummary, and the org.opennms.netmgt.model.{alarm,outage}
             subpackages. Before horizon 1.0.9, this was inherited transitively through
             events.api; horizon 1.0.9 marked core.model-api as <optional>true</optional>
             so it no longer propagates. Declared here explicitly with exclusions to
             keep the legacy eclipselink/hibernate-3/servicemix-spring chain out. -->
        <dependency>
            <groupId>org.opennms.core</groupId>
            <artifactId>org.opennms.core.model-api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>jaxb-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.hibernate</groupId><artifactId>hibernate-core</artifactId></exclusion>
                <exclusion><groupId>org.hibernate.javax.persistence</groupId><artifactId>hibernate-jpa-2.0-api</artifactId></exclusion>
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- Hibernate 3.x — compile-only for CategoryDao.getCriterionForCategorySetsUnion return type.
             The method always throws UnsupportedOperationException in CategoryDaoJpa; the jar is
             needed only so javac can resolve org.hibernate.criterion.Criterion. It is excluded from
             every other transitive path (opennms-dao-api, opennms-model) and absent from the
             Spring Boot BOM, so an explicit provided-scope entry is the only reliable option.
             It does NOT enter the fat JAR — spring-boot-maven-plugin only packages compile/runtime deps. -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Enlinkd enum types are now defined as inner classes in our Jakarta entities.
             No dependency on enlinkd-persistence-api needed. -->

        <!-- opennms-rrd-api — compile-only for ResourceTypeUtils.getResourcePathWithRepository
             signature. Horizon's features.timeseries-1.0.11 calls this static method on the
             jakarta ResourceTypeUtils and gets NoSuchMethodError if the signature is missing;
             see core/daemon-boot-collectd where features.timeseries already brings
             opennms-rrd-api transitively at runtime. `provided` scope mirrors hibernate-core
             and jaxb-api above — we only need the RrdRepository type for javac. -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-rrd-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- javax.xml.bind — compile-only for PrimaryTypeAdapter (extends XmlAdapter).
             Horizon's RequisitionInterface has @XmlJavaTypeAdapter(PrimaryTypeAdapter.class)
             baked into its bytecode; the adapter must extend the javax XmlAdapter at runtime.
             jaxb-api-2.3.1.jar is already on the Docker classpath (external layer). -->
        <dependency>
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            <version>2.3.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- Jackson for @JsonIgnoreProperties -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Boot 4.0 ships JUnit Jupiter 6.0.x but spring-boot-starter-test does not bring
             junit-platform-launcher transitively; maven-surefire-plugin 3.5.x requires it on
             the test classpath or discovery fails with "OutputDirectoryCreator not available".
             Version is BOM-managed (aligns with junit-platform-engine). -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.5</version>
                <configuration>
                    <includeJUnit5Engines>
                        <includeJUnit5Engine>junit-jupiter</includeJUnit5Engine>
                    </includeJUnit5Engines>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.junit.jupiter</groupId>
                        <artifactId>junit-jupiter-engine</artifactId>
                        <version>5.12.2</version>
                    </dependency>
                    <dependency>
                        <groupId>org.junit.platform</groupId>
                        <artifactId>junit-platform-launcher</artifactId>
                        <version>1.12.2</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>
</project>
