<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.deltav</groupId>
        <artifactId>delta-v-parent</artifactId>
        <version>1.3.0-rc9</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <groupId>org.deltav.core</groupId>
    <artifactId>org.opennms.core.event-forwarder-kafka</artifactId>
    <name>OpenNMS :: Core :: Event Forwarder Kafka</name>
    <description>
        EventForwarder implementation that enriches events locally and publishes
        to Kafka (FAULT events) or JMS MessageBus (IPC events).
    </description>

    <dependencies>
        <!-- Event API (EventForwarder, EventProcessor interfaces) -->
        <dependency>
            <groupId>org.opennms.features.events</groupId>
            <artifactId>org.opennms.features.events.api</artifactId>
        </dependency>

        <!-- Config API (EventConfDao interface) -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-config-api</artifactId>
        </dependency>

        <!-- DAO API (EventConfEventDao interface) -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-dao-api</artifactId>
        </dependency>

        <!-- Config (DefaultEventConfDao implementation — available at runtime via Karaf) -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-config</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- MessageBus API (IPC event routing) -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>org.opennms.core.messagebus.api</artifactId>
        </dependency>

        <!-- Event daemon (EventExpander, TsidAssigner, EventClassifier, IpcMessageConverter) -->
        <dependency>
            <groupId>org.opennms.features.events</groupId>
            <artifactId>org.opennms.features.events.daemon</artifactId>
        </dependency>

        <!-- Kafka consumer (EventDeserializer interface) -->
        <dependency>
            <groupId>org.opennms.features.events</groupId>
            <artifactId>org.opennms.features.events.kafka-consumer</artifactId>
        </dependency>

        <!-- Kafka client (standard, not ServiceMix OSGi bundle) -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Spring (for InitializingBean) -->
        <dependency>
            <groupId>org.apache.servicemix.bundles</groupId>
            <artifactId>org.apache.servicemix.bundles.spring-beans</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
