<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.deltav</groupId>
        <artifactId>delta-v-parent</artifactId>
        <version>1.2.0-rc4.1</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <groupId>org.deltav.core</groupId>
    <artifactId>org.opennms.core.daemon-common</artifactId>
    <name>OpenNMS :: Core :: Daemon Common</name>

    <dependencies>
        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Jackson XML — replaces JaxbUtils for eventconf XML deserialization.
             jackson-module-jaxb-annotations reads existing javax.xml.bind
             annotations on opennms-config-model Event class. -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-xml</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.module</groupId>
            <artifactId>jackson-module-jaxb-annotations</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <!-- Kafka -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>

        <!-- Horizon Dropwizard → Micrometer bridge.
             Surfaces kafkaRpcMetricRegistry meters at /actuator/prometheus
             for every daemon that imports KafkaRpcClientConfiguration. -->
        <dependency>
            <groupId>org.deltav.core</groupId>
            <artifactId>org.deltav.horizon-metric-bridge</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Micrometer Prometheus registry — required for Spring Boot Actuator
             to register the /actuator/prometheus endpoint at all. Without it,
             enabling 'prometheus' in management.endpoints.web.exposure.include
             silently 404s. Pulled into daemon-common so every Spring Boot
             daemon gets the endpoint by default. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- opennms-config-model — provides eventconf JAXB classes (Event, Events,
             EventMatchers, Partition) used by DaemonEventConfDao for event matching.
             No opennms-model dependency — safe for Spring Boot daemon classpath. -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-config-model</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-beans</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-context</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-core</artifactId></exclusion>
                <exclusion><groupId>org.slf4j</groupId><artifactId>slf4j-api</artifactId></exclusion>
                <exclusion><groupId>org.ops4j.pax.logging</groupId><artifactId>pax-logging-api</artifactId></exclusion>
                <exclusion><groupId>commons-logging</groupId><artifactId>commons-logging</artifactId></exclusion>
            </exclusions>
        </dependency>
        <!-- opennms-config-api — provides EventConfDao interface used by
             KafkaEventForwarder. opennms-model excluded; EventConfEvent/EventConfSource
             resolve from model-jakarta. -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-config-api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-beans</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-context</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-core</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-tx</artifactId></exclusion>
                <exclusion><groupId>org.slf4j</groupId><artifactId>slf4j-api</artifactId></exclusion>
                <exclusion><groupId>org.ops4j.pax.logging</groupId><artifactId>pax-logging-api</artifactId></exclusion>
                <exclusion><groupId>commons-logging</groupId><artifactId>commons-logging</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- JDBC (for JdbcTemplate in JdbcDistPollerDao, JdbcInterfaceToNodeCache) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- Jakarta entity types (OnmsNode, OnmsDistPoller, EventConfEvent, etc.)
             Replaces the former opennms-model compile dependency. The cycle that
             previously prevented this (model-jakarta → daemon-common) is broken:
             model-jakarta now depends on dao-jpa-support, not daemon-common. -->
        <dependency>
            <groupId>org.opennms.core</groupId>
            <artifactId>org.opennms.core.model-jakarta</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- DAO interfaces. opennms-model excluded — model-jakarta above supplies
             the same entity types in the same package. -->
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-dao-api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.opennms.core</groupId>
            <artifactId>org.opennms.core.daemon</artifactId>
            <exclusions>
                <!-- spring-dependencies and spring-web-dependencies pull ServiceMix Spring 4.2.x bundles
                     that conflict with Spring 7 provided by Spring Boot 4 -->
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-web-dependencies</artifactId></exclusion>
                <!-- opennms-model contains javax.persistence entities that clash with
                     model-jakarta (jakarta.persistence) under Hibernate 7 entity scanning.
                     AbstractServiceDaemon and SpringServiceDaemon (the only classes we use
                     from core/daemon) reference ServiceDaemon from model-api, not opennms-model. -->
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.opennms.features.events</groupId>
            <artifactId>org.opennms.features.events.api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.deltav.core</groupId>
            <artifactId>org.opennms.core.event-forwarder-kafka</artifactId>
            <version>${project.version}</version>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- Kafka RPC client (shared by Discovery, Pollerd, Collectd, Enlinkd, etc.) -->
        <dependency>
            <groupId>org.opennms.core.ipc.rpc</groupId>
            <artifactId>org.opennms.core.ipc.rpc.kafka</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-beans</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-context</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-core</artifactId></exclusion>
                <exclusion><groupId>org.apache.servicemix.bundles</groupId><artifactId>org.apache.servicemix.bundles.spring-tx</artifactId></exclusion>
                <exclusion><groupId>org.hibernate</groupId><artifactId>hibernate-core</artifactId></exclusion>
                <exclusion><groupId>org.slf4j</groupId><artifactId>slf4j-api</artifactId></exclusion>
                <exclusion><groupId>org.ops4j.pax.logging</groupId><artifactId>pax-logging-api</artifactId></exclusion>
                <exclusion><groupId>commons-logging</groupId><artifactId>commons-logging</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- RPC utils (RpcTargetHelper) -->
        <dependency>
            <groupId>org.opennms.core.ipc.rpc</groupId>
            <artifactId>org.opennms.core.ipc.rpc.utils</artifactId>
        </dependency>

        <!-- Tracing API (TracerRegistry interface for NoOpTracerRegistry) -->
        <dependency>
            <groupId>org.opennms.core.tracing</groupId>
            <artifactId>org.opennms.core.tracing.api</artifactId>
        </dependency>

        <!-- OpenTracing (required by KafkaRpcClientFactory) -->
        <dependency>
            <groupId>io.opentracing</groupId>
            <artifactId>opentracing-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentracing</groupId>
            <artifactId>opentracing-util</artifactId>
        </dependency>

        <!-- MATE API (EntityScopeProvider interface for NoOpEntityScopeProvider) -->
        <dependency>
            <groupId>org.opennms.core.mate</groupId>
            <artifactId>org.opennms.core.mate.api</artifactId>
        </dependency>

        <!-- SCV API + JCEKS implementation (for DaemonEntityScopeProvider) -->
        <dependency>
            <groupId>org.opennms.features.scv</groupId>
            <artifactId>org.opennms.features.scv.api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.opennms.features.scv</groupId>
            <artifactId>org.opennms.features.scv.jceks-impl</artifactId>
        </dependency>

        <!-- Geohash (for DaemonEntityScopeProvider node geohash computation) -->
        <dependency>
            <groupId>ch.hsr</groupId>
            <artifactId>geohash</artifactId>
        </dependency>

        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-detector-registry</artifactId>
            <exclusions>
                <!-- core.soa pulls spring-dependencies (ServiceMix Spring 4.2.x) -->
                <exclusion><groupId>org.opennms.core</groupId><artifactId>org.opennms.core.soa</artifactId></exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.opennms</groupId>
            <artifactId>opennms-provision-api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>mina-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>netty-dependencies</artifactId></exclusion>
                <!-- opennms-model excluded — model-jakarta supplies entity types -->
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- Collection API (ServiceCollectorRegistry interface) -->
        <dependency>
            <groupId>org.opennms.features.collection</groupId>
            <artifactId>org.opennms.features.collection.api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
                <exclusion><groupId>org.opennms</groupId><artifactId>opennms-model</artifactId></exclusion>
            </exclusions>
        </dependency>
        <!-- Poller API (ServiceMonitorRegistry interface) -->
        <dependency>
            <groupId>org.opennms.features.poller</groupId>
            <artifactId>org.opennms.features.poller.api</artifactId>
            <exclusions>
                <exclusion><groupId>org.opennms.dependencies</groupId><artifactId>spring-dependencies</artifactId></exclusion>
            </exclusions>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Jakarta XML Bind + Activation — needed by Hibernate 7's persistence.xml parser at test time -->
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.activation</groupId>
            <artifactId>jakarta.activation-api</artifactId>
            <version>2.1.3</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.eclipse.angus</groupId>
            <artifactId>angus-activation</artifactId>
            <version>2.0.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <!-- Exclude vintage engine to avoid classpath conflict
                     with the JUnit 4 vintage engine from the parent POM -->
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <profiles>
        <!--
            jakarta-transform profile: produces a secondary JAR artifact
            with classifier "jakarta" where all javax.* bytecode references
            are rewritten to jakarta.* using the Eclipse Transformer.

            This is the bridge mechanism for the Spring Boot 4 migration:
            - Shared modules (opennms-model, opennms-dao, etc.) keep javax.*
              in source so Karaf / Hibernate 3.6 consumers are unaffected.
            - Spring Boot daemon fat JARs activate this profile to get
              jakarta.* bytecode that Hibernate 6.x / Spring 7 require.

            Usage:
              mvn package -Pjakarta-transform

            The consuming daemon module (daemon-boot-alarmd) activates this
            profile and uses the transformer-maven-plugin on its fat JAR to
            rewrite embedded javax.* classes in one pass.
        -->
        <profile>
            <id>jakarta-transform</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.eclipse.transformer</groupId>
                        <artifactId>transformer-maven-plugin</artifactId>
                        <version>0.5.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <rules>
                                <jakartaDefaults>true</jakartaDefaults>
                                <renames>${project.basedir}/src/main/resources/META-INF/jakarta-rename.properties</renames>
                            </rules>
                        </configuration>
                        <executions>
                            <execution>
                                <id>transform-jar</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                                <configuration>
                                    <classifier>jakarta</classifier>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
